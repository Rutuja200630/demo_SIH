export default function AuthLogin() {
  return (
    <main className="container mx-auto py-8 max-w-md">
      <div className="p-6 rounded-2xl border border-white/10 bg-white/5 backdrop-blur">
        <div className="font-semibold mb-2">Login</div>
        <p className="text-sm text-foreground/70">Development-only auth will be added later. Use the dashboards to explore for now.</p>
      </div>
    </main>
  );
}
